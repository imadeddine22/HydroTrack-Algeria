{
  "name": "hydrotrack-backend",
  "version": "1.0.0",
  "description": "HydroTrack Algeria — Express.js REST API backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node -e \"require('dotenv').config(); const seed = require('./seed'); seed(process.env.MONGODB_URI).then(() => process.exit(0)).catch(e => { console.error(e); process.exit(1); })\""
  },
  "keywords": [
    "hydrotrack",
    "algeria",
    "water",
    "infrastructure"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "express-rate-limit": "^8.5.1",
    "helmet": "^8.1.0",
    "mongoose": "^8.0.0",
    "morgan": "^1.10.1"
  }
}
